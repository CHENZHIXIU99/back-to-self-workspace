import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.chenzhixiu.backtoself",
  appName: "BackToSelf Workspace",
  webDir: "out",
  backgroundColor: "#f4f1ea",
};

export default config;
